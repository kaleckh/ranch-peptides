# Henry's Peptides Agent Guide

Before changing this repository, read `docs/PROJECT_CONTEXT.md`, `docs/DECISIONS.md`, `docs/NEXT_STEPS.md`, `README.md`, and `package.json`. Preserve the research-only storefront positioning, static-export deployment contract, and current dark industrial visual system unless the user explicitly changes direction.

Use `npm run dev -- --webpack` for local development on this Windows machine. Run `npm run lint` and `npm run build` in proportion to the change. After meaningful work, append `docs/WORKLOG.md` and update the other memory files when their owned facts change.

<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->
